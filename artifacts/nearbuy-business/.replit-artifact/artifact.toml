kind = "mobile"
previewPath = "/"
title = "NearBuy Business"
version = "1.0.0"
id = "artifacts/nearbuy-business"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 20247

[services.development]
run = "pnpm --filter @workspace/nearbuy-business run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nearbuy-business", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/nearbuy-business", "run", "serve" ]

[services.env]
PORT = "20247"
BASE_PATH = "/"
